// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// Request message for
/// [BidiWriteObject][google.storage.v2.Storage.BidiWriteObject].
///
/// [google.storage.v2.Storage.BidiWriteObject]: <doc:Storage/bidiWriteObject(request:)>
public struct BidiWriteObjectRequest: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Required. The offset from the beginning of the object at which the data
  /// should be written.
  ///
  /// In the first `WriteObjectRequest` of a `WriteObject()` action, it
  /// indicates the initial offset for the `Write()` call. The value must be
  /// equal to the `persisted_size` that a call to `QueryWriteStatus()` would
  /// return (0 if this is the first write to the object).
  ///
  /// On subsequent calls, this value must be no larger than the sum of the
  /// first `write_offset` and the sizes of all `data` chunks sent previously on
  /// this stream.
  ///
  /// An invalid value causes an error.
  public var writeOffset: Swift.Int64 = Swift.Int64()

  /// Optional. Checksums for the complete object. If the checksums computed by
  /// the service don't match the specified checksums the call fails. May be
  /// provided in the last request (with finish_write set). For non-appendable
  /// objects only, may also be provided in the first request.
  public var objectChecksums: ObjectChecksums? = nil

  /// Optional. For each `BidiWriteObjectRequest` where `state_lookup` is `true`
  /// or the client closes the stream, the service sends a
  /// `BidiWriteObjectResponse` containing the current persisted size. The
  /// persisted size sent in responses covers all the bytes the server has
  /// persisted thus far and can be used to decide what data is safe for the
  /// client to drop. Note that the object's current size reported by the
  /// `BidiWriteObjectResponse` might lag behind the number of bytes written by
  /// the client. This field is ignored if `finish_write` is set to true.
  public var stateLookup: Swift.Bool = Swift.Bool()

  /// Optional. Persists data written on the stream, up to and including the
  /// current message, to permanent storage. This option should be used sparingly
  /// as it might reduce performance. Ongoing writes are periodically persisted
  /// on the server even when `flush` is not set. This field is ignored if
  /// `finish_write` is set to true since there's no need to checkpoint or flush
  /// if this message completes the write.
  public var flush: Swift.Bool = Swift.Bool()

  /// Optional. If `true`, this indicates that the write is complete. Sending any
  /// `WriteObjectRequest`s subsequent to one in which `finish_write` is `true`
  /// causes an error.
  /// For a non-resumable write (where the `upload_id` was not set in the first
  /// message), it is an error not to set this field in the final message of the
  /// stream.
  public var finishWrite: Swift.Bool = Swift.Bool()

  /// Optional. A set of parameters common to Storage API requests concerning an
  /// object.
  public var commonObjectRequestParams: CommonObjectRequestParams? = nil

  /// The first message of each stream should set one of the following.
  public var firstMessage: OneOf_FirstMessage? = nil

  /// A portion of the data for the object.
  public var data: OneOf_Data? = nil

  /// Initialize a new instance of `BidiWriteObjectRequest`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = BidiWriteObjectRequest().with { $0.uploadId = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case uploadId = "uploadId"
    case writeObjectSpec = "writeObjectSpec"
    case appendObjectSpec = "appendObjectSpec"
    case writeOffset = "writeOffset"
    case checksummedData = "checksummedData"
    case objectChecksums = "objectChecksums"
    case stateLookup = "stateLookup"
    case flush = "flush"
    case finishWrite = "finishWrite"
    case commonObjectRequestParams = "commonObjectRequestParams"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.writeOffset = try container.decode(Swift.Int64.self, forKey: .writeOffset)
    self.objectChecksums = try container.decodeIfPresent(
      ObjectChecksums.self, forKey: .objectChecksums)
    self.stateLookup = try container.decode(Swift.Bool.self, forKey: .stateLookup)
    self.flush = try container.decode(Swift.Bool.self, forKey: .flush)
    self.finishWrite = try container.decode(Swift.Bool.self, forKey: .finishWrite)
    self.commonObjectRequestParams = try container.decodeIfPresent(
      CommonObjectRequestParams.self, forKey: .commonObjectRequestParams)

    var firstMessage: OneOf_FirstMessage? = nil
    let firstMessageCheckAndSet = {
      if firstMessage != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'firstMessage'"))
      }
      firstMessage = $0
    }
    if let uploadId = try container.decodeIfPresent(Swift.String.self, forKey: .uploadId) {
      try firstMessageCheckAndSet(.uploadId(uploadId))
    }
    if let writeObjectSpec = try container.decodeIfPresent(
      WriteObjectSpec?.self, forKey: .writeObjectSpec)
    {
      try firstMessageCheckAndSet(.writeObjectSpec(writeObjectSpec))
    }
    if let appendObjectSpec = try container.decodeIfPresent(
      AppendObjectSpec?.self, forKey: .appendObjectSpec)
    {
      try firstMessageCheckAndSet(.appendObjectSpec(appendObjectSpec))
    }
    self.firstMessage = firstMessage

    var data: OneOf_Data? = nil
    let dataCheckAndSet = {
      if data != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath, debugDescription: "Multiple values set for oneof 'data'"
          ))
      }
      data = $0
    }
    if let checksummedData = try container.decodeIfPresent(
      ChecksummedData?.self, forKey: .checksummedData)
    {
      try dataCheckAndSet(.checksummedData(checksummedData))
    }
    self.data = data
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.writeOffset, forKey: .writeOffset)
    try container.encode(self.objectChecksums, forKey: .objectChecksums)
    try container.encode(self.stateLookup, forKey: .stateLookup)
    try container.encode(self.flush, forKey: .flush)
    try container.encode(self.finishWrite, forKey: .finishWrite)
    try container.encode(self.commonObjectRequestParams, forKey: .commonObjectRequestParams)

    if let choice = self.firstMessage {
      switch choice {
      case .uploadId(let value):
        try container.encode(value, forKey: .uploadId)
      case .writeObjectSpec(let value):
        try container.encode(value, forKey: .writeObjectSpec)
      case .appendObjectSpec(let value):
        try container.encode(value, forKey: .appendObjectSpec)
      }
    }

    if let choice = self.data {
      switch choice {
      case .checksummedData(let value):
        try container.encode(value, forKey: .checksummedData)
      }
    }
  }

  /// The first message of each stream should set one of the following.
  public enum OneOf_FirstMessage: Codable, Equatable, Sendable {
    /// For resumable uploads. This should be the `upload_id` returned from a
    /// call to `StartResumableWriteResponse`.
    case uploadId(Swift.String)
    /// For non-resumable uploads. Describes the overall upload, including the
    /// destination bucket and object name, preconditions, etc.
    indirect case writeObjectSpec(WriteObjectSpec?)
    /// For appendable uploads. Describes the object to append to.
    indirect case appendObjectSpec(AppendObjectSpec?)
  }

  /// A portion of the data for the object.
  public enum OneOf_Data: Codable, Equatable, Sendable {
    /// The data to insert. If a crc32c checksum is provided that doesn't match
    /// the checksum computed by the service, the request fails.
    indirect case checksummedData(ChecksummedData?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.storage.v2.BidiWriteObjectRequest"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
