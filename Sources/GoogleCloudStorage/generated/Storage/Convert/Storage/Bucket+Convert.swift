// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudGax
internal import StorageProtos
internal import SwiftProtobuf
import GoogleCloudWKT
import GoogleType
internal import GoogleCloudWKTConvert

extension Bucket {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket

  internal init(proto: ProtoType) throws {
    self.init()
    self.name = proto.name
    self.bucketId = proto.bucketID
    self.etag = proto.etag
    self.project = proto.project
    self.metageneration = proto.metageneration
    self.location = proto.location
    self.locationType = proto.locationType
    self.storageClass = proto.storageClass
    self.rpo = proto.rpo
    self.acl = try proto.acl.map { try .init(proto: $0) }
    self.defaultObjectAcl = try proto.defaultObjectAcl.map { try .init(proto: $0) }
    self.lifecycle = proto.hasLifecycle ? try .init(proto: proto.lifecycle) : nil
    self.createTime = proto.hasCreateTime ? try .init(proto: proto.createTime) : nil
    self.cors = try proto.cors.map { try .init(proto: $0) }
    self.updateTime = proto.hasUpdateTime ? try .init(proto: proto.updateTime) : nil
    self.defaultEventBasedHold = proto.defaultEventBasedHold
    self.labels = proto.labels
    self.website = proto.hasWebsite ? try .init(proto: proto.website) : nil
    self.versioning = proto.hasVersioning ? try .init(proto: proto.versioning) : nil
    self.logging = proto.hasLogging ? try .init(proto: proto.logging) : nil
    self.owner = proto.hasOwner ? try .init(proto: proto.owner) : nil
    self.encryption = proto.hasEncryption ? try .init(proto: proto.encryption) : nil
    self.billing = proto.hasBilling ? try .init(proto: proto.billing) : nil
    self.retentionPolicy = proto.hasRetentionPolicy ? try .init(proto: proto.retentionPolicy) : nil
    self.iamConfig = proto.hasIamConfig ? try .init(proto: proto.iamConfig) : nil
    self.satisfiesPzs = proto.satisfiesPzs
    self.customPlacementConfig =
      proto.hasCustomPlacementConfig ? try .init(proto: proto.customPlacementConfig) : nil
    self.autoclass = proto.hasAutoclass ? try .init(proto: proto.autoclass) : nil
    self.hierarchicalNamespace =
      proto.hasHierarchicalNamespace ? try .init(proto: proto.hierarchicalNamespace) : nil
    self.softDeletePolicy =
      proto.hasSoftDeletePolicy ? try .init(proto: proto.softDeletePolicy) : nil
    self.objectRetention = proto.hasObjectRetention ? try .init(proto: proto.objectRetention) : nil
    self.ipFilter = proto.hasIpFilter ? try .init(proto: proto.ipFilter) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.name = self.name
    proto.bucketID = self.bucketId
    proto.etag = self.etag
    proto.project = self.project
    proto.metageneration = self.metageneration
    proto.location = self.location
    proto.locationType = self.locationType
    proto.storageClass = self.storageClass
    proto.rpo = self.rpo
    proto.acl = try self.acl.map { try $0.toProto() }
    proto.defaultObjectAcl = try self.defaultObjectAcl.map { try $0.toProto() }
    if let lifecycle = self.lifecycle { proto.lifecycle = try lifecycle.toProto() }
    if let createTime = self.createTime { proto.createTime = try createTime.toProto() }
    proto.cors = try self.cors.map { try $0.toProto() }
    if let updateTime = self.updateTime { proto.updateTime = try updateTime.toProto() }
    proto.defaultEventBasedHold = self.defaultEventBasedHold
    proto.labels = self.labels
    if let website = self.website { proto.website = try website.toProto() }
    if let versioning = self.versioning { proto.versioning = try versioning.toProto() }
    if let logging = self.logging { proto.logging = try logging.toProto() }
    if let owner = self.owner { proto.owner = try owner.toProto() }
    if let encryption = self.encryption { proto.encryption = try encryption.toProto() }
    if let billing = self.billing { proto.billing = try billing.toProto() }
    if let retentionPolicy = self.retentionPolicy {
      proto.retentionPolicy = try retentionPolicy.toProto()
    }
    if let iamConfig = self.iamConfig { proto.iamConfig = try iamConfig.toProto() }
    proto.satisfiesPzs = self.satisfiesPzs
    if let customPlacementConfig = self.customPlacementConfig {
      proto.customPlacementConfig = try customPlacementConfig.toProto()
    }
    if let autoclass = self.autoclass { proto.autoclass = try autoclass.toProto() }
    if let hierarchicalNamespace = self.hierarchicalNamespace {
      proto.hierarchicalNamespace = try hierarchicalNamespace.toProto()
    }
    if let softDeletePolicy = self.softDeletePolicy {
      proto.softDeletePolicy = try softDeletePolicy.toProto()
    }
    if let objectRetention = self.objectRetention {
      proto.objectRetention = try objectRetention.toProto()
    }
    if let ipFilter = self.ipFilter { proto.ipFilter = try ipFilter.toProto() }
    return proto
  }
}

extension Bucket.Billing {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Billing

  internal init(proto: ProtoType) throws {
    self.init()
    self.requesterPays = proto.requesterPays
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.requesterPays = self.requesterPays
    return proto
  }
}

extension Bucket.Cors {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Cors

  internal init(proto: ProtoType) throws {
    self.init()
    self.origin = proto.origin
    self.method = proto.method
    self.responseHeader = proto.responseHeader
    self.maxAgeSeconds = proto.maxAgeSeconds
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.origin = self.origin
    proto.method = self.method
    proto.responseHeader = self.responseHeader
    proto.maxAgeSeconds = self.maxAgeSeconds
    return proto
  }
}

extension Bucket.Encryption {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Encryption

  internal init(proto: ProtoType) throws {
    self.init()
    self.defaultKmsKey = proto.defaultKmsKey
    self.googleManagedEncryptionEnforcementConfig =
      proto.hasGoogleManagedEncryptionEnforcementConfig
      ? try .init(proto: proto.googleManagedEncryptionEnforcementConfig) : nil
    self.customerManagedEncryptionEnforcementConfig =
      proto.hasCustomerManagedEncryptionEnforcementConfig
      ? try .init(proto: proto.customerManagedEncryptionEnforcementConfig) : nil
    self.customerSuppliedEncryptionEnforcementConfig =
      proto.hasCustomerSuppliedEncryptionEnforcementConfig
      ? try .init(proto: proto.customerSuppliedEncryptionEnforcementConfig) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.defaultKmsKey = self.defaultKmsKey
    if let googleManagedEncryptionEnforcementConfig = self.googleManagedEncryptionEnforcementConfig
    {
      proto.googleManagedEncryptionEnforcementConfig =
        try googleManagedEncryptionEnforcementConfig.toProto()
    }
    if let customerManagedEncryptionEnforcementConfig = self
      .customerManagedEncryptionEnforcementConfig
    {
      proto.customerManagedEncryptionEnforcementConfig =
        try customerManagedEncryptionEnforcementConfig.toProto()
    }
    if let customerSuppliedEncryptionEnforcementConfig = self
      .customerSuppliedEncryptionEnforcementConfig
    {
      proto.customerSuppliedEncryptionEnforcementConfig =
        try customerSuppliedEncryptionEnforcementConfig.toProto()
    }
    return proto
  }
}

extension Bucket.Encryption.GoogleManagedEncryptionEnforcementConfig {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Encryption
    .GoogleManagedEncryptionEnforcementConfig

  internal init(proto: ProtoType) throws {
    self.init()
    self.restrictionMode = proto.hasRestrictionMode ? proto.restrictionMode : nil
    self.effectiveTime = proto.hasEffectiveTime ? try .init(proto: proto.effectiveTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let restrictionMode = self.restrictionMode { proto.restrictionMode = restrictionMode }
    if let effectiveTime = self.effectiveTime { proto.effectiveTime = try effectiveTime.toProto() }
    return proto
  }
}

extension Bucket.Encryption.CustomerManagedEncryptionEnforcementConfig {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Encryption
    .CustomerManagedEncryptionEnforcementConfig

  internal init(proto: ProtoType) throws {
    self.init()
    self.restrictionMode = proto.hasRestrictionMode ? proto.restrictionMode : nil
    self.effectiveTime = proto.hasEffectiveTime ? try .init(proto: proto.effectiveTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let restrictionMode = self.restrictionMode { proto.restrictionMode = restrictionMode }
    if let effectiveTime = self.effectiveTime { proto.effectiveTime = try effectiveTime.toProto() }
    return proto
  }
}

extension Bucket.Encryption.CustomerSuppliedEncryptionEnforcementConfig {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Encryption
    .CustomerSuppliedEncryptionEnforcementConfig

  internal init(proto: ProtoType) throws {
    self.init()
    self.restrictionMode = proto.hasRestrictionMode ? proto.restrictionMode : nil
    self.effectiveTime = proto.hasEffectiveTime ? try .init(proto: proto.effectiveTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let restrictionMode = self.restrictionMode { proto.restrictionMode = restrictionMode }
    if let effectiveTime = self.effectiveTime { proto.effectiveTime = try effectiveTime.toProto() }
    return proto
  }
}

extension Bucket.IamConfig {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.IamConfig

  internal init(proto: ProtoType) throws {
    self.init()
    self.uniformBucketLevelAccess =
      proto.hasUniformBucketLevelAccess ? try .init(proto: proto.uniformBucketLevelAccess) : nil
    self.publicAccessPrevention = proto.publicAccessPrevention
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let uniformBucketLevelAccess = self.uniformBucketLevelAccess {
      proto.uniformBucketLevelAccess = try uniformBucketLevelAccess.toProto()
    }
    proto.publicAccessPrevention = self.publicAccessPrevention
    return proto
  }
}

extension Bucket.IamConfig.UniformBucketLevelAccess {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.IamConfig
    .UniformBucketLevelAccess

  internal init(proto: ProtoType) throws {
    self.init()
    self.enabled = proto.enabled
    self.lockTime = proto.hasLockTime ? try .init(proto: proto.lockTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.enabled = self.enabled
    if let lockTime = self.lockTime { proto.lockTime = try lockTime.toProto() }
    return proto
  }
}

extension Bucket.Lifecycle {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Lifecycle

  internal init(proto: ProtoType) throws {
    self.init()
    self.rule = try proto.rule.map { try .init(proto: $0) }
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.rule = try self.rule.map { try $0.toProto() }
    return proto
  }
}

extension Bucket.Lifecycle.Rule {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Lifecycle.Rule

  internal init(proto: ProtoType) throws {
    self.init()
    self.action = proto.hasAction ? try .init(proto: proto.action) : nil
    self.condition = proto.hasCondition ? try .init(proto: proto.condition) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let action = self.action { proto.action = try action.toProto() }
    if let condition = self.condition { proto.condition = try condition.toProto() }
    return proto
  }
}

extension Bucket.Lifecycle.Rule.Action {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Lifecycle.Rule.Action

  internal init(proto: ProtoType) throws {
    self.init()
    self.type = proto.type
    self.storageClass = proto.storageClass
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.type = self.type
    proto.storageClass = self.storageClass
    return proto
  }
}

extension Bucket.Lifecycle.Rule.Condition {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Lifecycle.Rule.Condition

  internal init(proto: ProtoType) throws {
    self.init()
    self.ageDays = proto.hasAgeDays ? proto.ageDays : nil
    self.createdBefore = proto.hasCreatedBefore ? try .init(proto: proto.createdBefore) : nil
    self.isLive = proto.hasIsLive ? proto.isLive : nil
    self.numNewerVersions = proto.hasNumNewerVersions ? proto.numNewerVersions : nil
    self.matchesStorageClass = proto.matchesStorageClass
    self.daysSinceCustomTime = proto.hasDaysSinceCustomTime ? proto.daysSinceCustomTime : nil
    self.customTimeBefore =
      proto.hasCustomTimeBefore ? try .init(proto: proto.customTimeBefore) : nil
    self.daysSinceNoncurrentTime =
      proto.hasDaysSinceNoncurrentTime ? proto.daysSinceNoncurrentTime : nil
    self.noncurrentTimeBefore =
      proto.hasNoncurrentTimeBefore ? try .init(proto: proto.noncurrentTimeBefore) : nil
    self.matchesPrefix = proto.matchesPrefix
    self.matchesSuffix = proto.matchesSuffix
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let ageDays = self.ageDays { proto.ageDays = ageDays }
    if let createdBefore = self.createdBefore { proto.createdBefore = try createdBefore.toProto() }
    if let isLive = self.isLive { proto.isLive = isLive }
    if let numNewerVersions = self.numNewerVersions { proto.numNewerVersions = numNewerVersions }
    proto.matchesStorageClass = self.matchesStorageClass
    if let daysSinceCustomTime = self.daysSinceCustomTime {
      proto.daysSinceCustomTime = daysSinceCustomTime
    }
    if let customTimeBefore = self.customTimeBefore {
      proto.customTimeBefore = try customTimeBefore.toProto()
    }
    if let daysSinceNoncurrentTime = self.daysSinceNoncurrentTime {
      proto.daysSinceNoncurrentTime = daysSinceNoncurrentTime
    }
    if let noncurrentTimeBefore = self.noncurrentTimeBefore {
      proto.noncurrentTimeBefore = try noncurrentTimeBefore.toProto()
    }
    proto.matchesPrefix = self.matchesPrefix
    proto.matchesSuffix = self.matchesSuffix
    return proto
  }
}

extension Bucket.Logging {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Logging

  internal init(proto: ProtoType) throws {
    self.init()
    self.logBucket = proto.logBucket
    self.logObjectPrefix = proto.logObjectPrefix
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.logBucket = self.logBucket
    proto.logObjectPrefix = self.logObjectPrefix
    return proto
  }
}

extension Bucket.ObjectRetention {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.ObjectRetention

  internal init(proto: ProtoType) throws {
    self.init()
    self.enabled = proto.enabled
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.enabled = self.enabled
    return proto
  }
}

extension Bucket.RetentionPolicy {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.RetentionPolicy

  internal init(proto: ProtoType) throws {
    self.init()
    self.effectiveTime = proto.hasEffectiveTime ? try .init(proto: proto.effectiveTime) : nil
    self.isLocked = proto.isLocked
    self.retentionDuration =
      proto.hasRetentionDuration ? try .init(proto: proto.retentionDuration) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let effectiveTime = self.effectiveTime { proto.effectiveTime = try effectiveTime.toProto() }
    proto.isLocked = self.isLocked
    if let retentionDuration = self.retentionDuration {
      proto.retentionDuration = try retentionDuration.toProto()
    }
    return proto
  }
}

extension Bucket.SoftDeletePolicy {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.SoftDeletePolicy

  internal init(proto: ProtoType) throws {
    self.init()
    self.retentionDuration =
      proto.hasRetentionDuration ? try .init(proto: proto.retentionDuration) : nil
    self.effectiveTime = proto.hasEffectiveTime ? try .init(proto: proto.effectiveTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let retentionDuration = self.retentionDuration {
      proto.retentionDuration = try retentionDuration.toProto()
    }
    if let effectiveTime = self.effectiveTime { proto.effectiveTime = try effectiveTime.toProto() }
    return proto
  }
}

extension Bucket.Versioning {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Versioning

  internal init(proto: ProtoType) throws {
    self.init()
    self.enabled = proto.enabled
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.enabled = self.enabled
    return proto
  }
}

extension Bucket.Website {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Website

  internal init(proto: ProtoType) throws {
    self.init()
    self.mainPageSuffix = proto.mainPageSuffix
    self.notFoundPage = proto.notFoundPage
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.mainPageSuffix = self.mainPageSuffix
    proto.notFoundPage = self.notFoundPage
    return proto
  }
}

extension Bucket.CustomPlacementConfig {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.CustomPlacementConfig

  internal init(proto: ProtoType) throws {
    self.init()
    self.dataLocations = proto.dataLocations
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.dataLocations = self.dataLocations
    return proto
  }
}

extension Bucket.Autoclass {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.Autoclass

  internal init(proto: ProtoType) throws {
    self.init()
    self.enabled = proto.enabled
    self.toggleTime = proto.hasToggleTime ? try .init(proto: proto.toggleTime) : nil
    self.terminalStorageClass = proto.hasTerminalStorageClass ? proto.terminalStorageClass : nil
    self.terminalStorageClassUpdateTime =
      proto.hasTerminalStorageClassUpdateTime
      ? try .init(proto: proto.terminalStorageClassUpdateTime) : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.enabled = self.enabled
    if let toggleTime = self.toggleTime { proto.toggleTime = try toggleTime.toProto() }
    if let terminalStorageClass = self.terminalStorageClass {
      proto.terminalStorageClass = terminalStorageClass
    }
    if let terminalStorageClassUpdateTime = self.terminalStorageClassUpdateTime {
      proto.terminalStorageClassUpdateTime = try terminalStorageClassUpdateTime.toProto()
    }
    return proto
  }
}

extension Bucket.IpFilter {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.IpFilter

  internal init(proto: ProtoType) throws {
    self.init()
    self.mode = proto.hasMode ? proto.mode : nil
    self.publicNetworkSource =
      proto.hasPublicNetworkSource ? try .init(proto: proto.publicNetworkSource) : nil
    self.vpcNetworkSources = try proto.vpcNetworkSources.map { try .init(proto: $0) }
    self.allowCrossOrgVpcs = proto.allowCrossOrgVpcs
    self.allowAllServiceAgentAccess =
      proto.hasAllowAllServiceAgentAccess ? proto.allowAllServiceAgentAccess : nil
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let mode = self.mode { proto.mode = mode }
    if let publicNetworkSource = self.publicNetworkSource {
      proto.publicNetworkSource = try publicNetworkSource.toProto()
    }
    proto.vpcNetworkSources = try self.vpcNetworkSources.map { try $0.toProto() }
    proto.allowCrossOrgVpcs = self.allowCrossOrgVpcs
    if let allowAllServiceAgentAccess = self.allowAllServiceAgentAccess {
      proto.allowAllServiceAgentAccess = allowAllServiceAgentAccess
    }
    return proto
  }
}

extension Bucket.IpFilter.PublicNetworkSource {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.IpFilter.PublicNetworkSource

  internal init(proto: ProtoType) throws {
    self.init()
    self.allowedIpCidrRanges = proto.allowedIpCidrRanges
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.allowedIpCidrRanges = self.allowedIpCidrRanges
    return proto
  }
}

extension Bucket.IpFilter.VpcNetworkSource {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.IpFilter.VpcNetworkSource

  internal init(proto: ProtoType) throws {
    self.init()
    self.network = proto.hasNetwork ? proto.network : nil
    self.allowedIpCidrRanges = proto.allowedIpCidrRanges
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    if let network = self.network { proto.network = network }
    proto.allowedIpCidrRanges = self.allowedIpCidrRanges
    return proto
  }
}

extension Bucket.HierarchicalNamespace {
  internal typealias ProtoType = StorageProtos.Google_Storage_V2_Bucket.HierarchicalNamespace

  internal init(proto: ProtoType) throws {
    self.init()
    self.enabled = proto.enabled
  }

  internal func toProto() throws -> ProtoType {
    var proto = ProtoType()
    proto.enabled = self.enabled
    return proto
  }
}
